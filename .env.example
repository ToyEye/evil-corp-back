# App
PORT=3000
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Postgres (docker-compose)
POSTGRES_USER=evilcorp
POSTGRES_PASSWORD=evilcorp
POSTGRES_DB=evilcorp
DATABASE_URL=postgresql://evilcorp:evilcorp@localhost:5433/evilcorp?schema=public

# Seed note: all users use password Password123!
# Host port 5433 maps to container 5432 (avoids clash with local PostgreSQL on 5432)
