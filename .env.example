# App
PORT=3000
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Postgres (docker-compose)
POSTGRES_USER=evilcorp
POSTGRES_PASSWORD=evilcorp
POSTGRES_DB=evilcorp
DATABASE_URL=postgresql://evilcorp:evilcorp@localhost:5432/evilcorp?schema=public

# Seed note (applied later): all users use password Password123!
