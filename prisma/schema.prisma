// Multi-tenant schema will be expanded in a later step.
// IDs are strings to match frontend dummy data 1:1.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
